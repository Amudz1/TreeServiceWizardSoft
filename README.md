# 🌳 Tree Service API

Минималистичный веб-сервис на ASP.NET Core для работы с древовидной иерархической структурой данных.

[![.NET](https://img.shields.io/badge/.NET-8.0-purple)](https://dotnet.microsoft.com/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Docker](https://img.shields.io/badge/docker-ready-blue)](https://www.docker.com/)

## ✨ Возможности

- ✅ Древовидная иерархическая модель данных без ограничения уровня вложенности
- ✅ REST API для CRUD операций
- ✅ Поддержка транзакций при изменении иерархии
- ✅ Защита от создания циклических ссылок
- ✅ JWT аутентификация
- ✅ Авторизация по ролям (Admin/User)
- ✅ Экспорт дерева в JSON
- ✅ SQLite база данных
- ✅ Docker поддержка
- ✅ Swagger UI для документации API

## 🛠️ Технологии

- **ASP.NET Core 8.0** - основной фреймворк
- **Entity Framework Core** - ORM для работы с БД
- **SQLite** - легковесная БД
- **JWT Bearer** - аутентификация
- **Swagger/OpenAPI** - документация API
- **Docker** - контейнеризация

## 🚀 Быстрый старт

### С помощью Docker (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/TreeService.git
cd TreeService

# Запустите с Docker
docker-compose up --build
```

Приложение доступно по адресу: **http://localhost:8080**

### Локальный запуск

```bash
# Требуется .NET 8.0 SDK
# https://dotnet.microsoft.com/download/dotnet/8.0

# Восстановите зависимости
dotnet restore

# Запустите приложение
dotnet run
```

## 👥 Тестовые пользователи

После первого запуска автоматически создаются:

| Username | Password | Роль | Права доступа |
|----------|----------|------|---------------|
| `admin` | `admin123` | Admin | Полный доступ (CRUD) |
| `user` | `user123` | User | Чтение и создание |

## 📚 API Endpoints

### Аутентификация

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/api/auth/register` | Регистрация нового пользователя |
| `POST` | `/api/auth/login` | Вход в систему (получение JWT токена) |

### Узлы дерева

*Все endpoints требуют аутентификации (JWT Bearer token)*

| Метод | Endpoint | Описание | Права |
|-------|----------|----------|-------|
| `GET` | `/api/nodes` | Получить все узлы (плоский список) | User, Admin |
| `GET` | `/api/nodes/{id}` | Получить узел по ID | User, Admin |
| `GET` | `/api/nodes/tree?rootId={id}` | Получить дерево (иерархическая структура) | User, Admin |
| `POST` | `/api/nodes` | Создать новый узел | User, Admin |
| `PUT` | `/api/nodes/{id}` | Обновить узел | **Admin only** |
| `DELETE` | `/api/nodes/{id}` | Удалить узел | **Admin only** |
| `GET` | `/api/nodes/export?rootId={id}` | Экспортировать дерево в JSON | User, Admin |

## 📖 Использование через Swagger UI

1. Откройте http://localhost:8080 в браузере
2. Выполните логин через `POST /api/auth/login` с тестовыми учетными данными
3. Скопируйте полученный `token` из ответа
4. Нажмите кнопку **"Authorize"** (иконка замка) вверху
5. Вставьте токен (без префикса "Bearer")
6. Нажмите **"Authorize"** и **"Close"**
7. Теперь можете использовать все API endpoints

## 💡 Примеры использования

### 1. Вход в систему

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin123"}'
```

**Ответ:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "username": "admin",
  "role": "Admin"
}
```

### 2. Создание корневого узла

```bash
curl -X POST http://localhost:8080/api/nodes \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Компания",
    "description": "Головная организация"
  }'
```

### 3. Создание дочернего узла

```bash
curl -X POST http://localhost:8080/api/nodes \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Разработка",
    "description": "Отдел разработки ПО",
    "parentId": 1
  }'
```

### 4. Получение дерева

```bash
curl -X GET http://localhost:8080/api/nodes/tree \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Результат:**
```json
{
  "id": 1,
  "name": "Компания",
  "description": "Головная организация",
  "children": [
    {
      "id": 2,
      "name": "Разработка",
      "description": "Отдел разработки ПО",
      "children": []
    }
  ]
}
```

## 🔐 Особенности безопасности

- Пароли хешируются с использованием SHA256
- JWT токены для stateless аутентификации
- Авторизация по ролям защищает критические операции
- Валидация входных данных на уровне DTO
- Транзакционная целостность данных

## 🏗️ Архитектура проекта

```
TreeService/
├── Controllers/          # API контроллеры
│   ├── AuthController.cs
│   └── NodesController.cs
├── Data/                # Контекст базы данных
│   └── ApplicationDbContext.cs
├── DTOs/                # Data Transfer Objects
│   └── Dtos.cs
├── Models/              # Модели данных
│   ├── TreeNode.cs
│   └── User.cs
├── Services/            # Бизнес-логика
│   ├── AuthService.cs
│   └── TreeService.cs
├── appsettings.json     # Конфигурация
├── Program.cs           # Точка входа
├── Dockerfile           # Docker образ
└── docker-compose.yml   # Docker Compose
```

## 🧪 Тестирование

Примеры тестовых сценариев доступны в файле [TESTING.md](TESTING.md)

## 📝 Требования

- .NET 8.0 SDK (для локального запуска)
- Docker и Docker Compose (для запуска в контейнере)

## 🤝 Вклад в проект

Contributions, issues и feature requests приветствуются!

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).

## 👨‍💻 Автор

Создано как демонстрационный проект уровня Junior+

---

⭐ Если проект был полезен, поставьте звезду!
